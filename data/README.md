# Data directory

The CICDDoS2019 files are not redistributed in this repository. Download them
from the official Canadian Institute for Cybersecurity source, retain their
original filenames, and place the extracted CSV files here. Do not commit the
raw dataset.

The preprocessing command writes a SHA-256 provenance manifest so that the
exact release and files used can be audited.
